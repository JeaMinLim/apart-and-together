# 따로또같이 — 설계 문서 (Design Document)

> 이 문서는 "따로또같이" 프로젝트의 핵심 설계 결정을 시간순으로 기록합니다. 아직 구현 전, 브레인스토밍/구조 설계 단계의 스냅샷입니다.

**진행 상태**: 이름 확정, 저장소(`apart-and-together`) 생성 및 README/LICENSE 커밋 완료. 1차 설계 결정(§2.1~§2.14) 후 초대/통신 메커니즘 구체화(§2.15~§2.23): 로컬 에이전트+MCP(§2.18), NAT 처리는 자체 릴레이(§2.19)에서 §2.13 백엔드 우편함 방식으로 대체(§2.20), PKI 구현은 libsodium(§2.21), git 커밋 서명 필수화(§2.22, 키 발급·등록 자동화), 배포는 1차로 저장소 클론 방식(§2.23, PyInstaller는 나중으로 미룸). 2차 라운드 미결 항목 전부 해소. Phase 0~3 구현 계획(§4) 수립, 착수 시점 미정.

---

## 1. 작업 개요 및 목표 (Objective)

여러 명이 각자 자신의 AI(서로 다른 벤더/구독 가능)를 가지고 있다고 가정한다. 핵심 아이디어:

1. 누군가 새로운 구현 목표를 **게시판처럼** 올린다.
2. **아는 사람들을 초대**하고, 초대받은 사람이 **수락(opt-in)** 한다 — 불특정 다수에게 공개되는 게시판이 아니라 서로 아는 사람들 사이의 초대제(§2.9 참고).
3. 과제를 올린 사람의 AI(**호스트 AI**)가 참여자들에게 일감을 나눠준다.
4. 각자 자기 AI로 맡은 부분을 처리한다.
5. 호스트 AI가 결과물을 **취합**해서 전체 목표(추론 또는 코딩)를 완성해간다.
6. 각 참여자가 실제로 기여한 부분에는 반드시 **기여자 귀속(attribution)**이 남아야 한다.

**포지셔닝**: [ModueHarness](https://github.com/JeaMinLim/ModueHarness)/[ModueAgent](https://github.com/JeaMinLim/ModueAgent)와는 **완전히 독립된 별개 프로젝트**다(§2.10). 이번 설계에서 재사용한 건 그 두 프로젝트의 설계 원칙(untrusted cognition 전제, 결정론적 검증, 자원 예산)이지 코드/런타임이 아니다.

---

## 2. 핵심 결정 사항 (Decision Log)

### 2.1. 병합 전 호스트 AI 검증 필수

참여자가 제출한 결과물을 호스트 AI가 **그대로 신뢰하고 병합하지 않는다** — 반드시 병합 전에 검증한다.

**왜**: 이 시스템은 서로 다른 사람·서로 다른 AI 벤더가 하나의 결과물에 기여하는 구조라, 한 명의 부실하거나 악의적인 기여가 전체를 오염시킬 수 있음. ModueAgent 프로젝트에서 이미 다룬 "신뢰할 수 없는 인지(untrusted cognition)" 문제와 같은 축이지만, 여기서는 도구 오남용이 아니라 **참여자 간 상호 신뢰**가 대상이라는 점이 다름.

### 2.2. 검증은 두 층위로 구성

1. **안전성 검증**: 제출된 코드/결과물에 악성 로직이 없는지. ModueAgent의 `AgentSecurityAuditor`(AST 기반 위험 호출 스캔) 패턴을 참고해 이 프로젝트 안에서 독립적으로 재구현한다(§2.13, Phase 1 참고).
2. **과제 적합성 검증**: 제출물이 실제로 맡은 하위 과제를 해결했는지. **과제를 참여자에게 나눠줄 때부터 기계적으로 채점 가능한 수락 기준(테스트 케이스, 인터페이스 스펙)을 명시해야만** 호스트 AI가 자동으로 검증할 수 있다는 전제가 붙음. 기준이 모호하면 검증이 사람 손으로 되돌아간다.

**인정하고 가는 한계**: 코드 과제는 테스트 케이스로 pass/fail이 기계적으로 명확하지만, 추론/분석 과제의 "체크리스트 항목 충족 여부"는 호스트 AI가 다시 판단해야 하는 LLM-as-judge 방식이라 코드만큼 확실하지 않다.

### 2.3. 검증 실패 시: 1회 재시도 → 재할당, 둘 다 상한 있음

- 검증 실패 시 원 기여자에게 **재시도 기회 1회**를 준다.
- 재시도도 실패하면 **재할당**한다.
- 재시도 횟수와 재할당 총 인원수 모두 **명시적 상한**을 둔다.

**왜**: 무제한 재시도/재할당은 Denial-of-Wallet(자원 고갈) 함정과 같다 — 과제가 영원히 표류하는 것을 막기 위해 자원 예산 개념을 사람/에이전트의 "시도 횟수"에도 그대로 적용한다.

재시도/재할당 시 실패 사유를 **블랙박스로 감추지 않고 구조화해서 다음 시도자에게 전달**한다(보안 이슈 위치, 미충족 수락 기준 항목 등).

### 2.4. 재할당 시 원본 시도 내용(코드) 전체를 다음 참여자에게 공개, 이어서 작업

실패 사유 요약만 주는 게 아니라 **원 기여자가 작성한 코드/결과물 자체를 통째로** 다음 참여자에게 넘기고, 그 위에서 이어가게 한다.

### 2.5. 기여 귀속(attribution)은 git 자체로 해결 — 별도 시스템 불필요

2.4 결정 덕분에 "실패한 기여자에게 부분 기여를 인정해줄 것인가"라는 어려운 문제가 자연스럽게 풀린다:

- 재할당 = 원 기여자의 마지막 커밋에서 **새 브랜치를 파서**, 다음 참여자가 그 위에 자기 이름으로 커밋을 쌓는 것으로 구현.
- 최종 병합본은 `git blame`/`git log`로 "이 줄은 A가 썼고, 이 줄은 B가 고쳤다"가 **자동으로** 남는다.
- 코드가 아닌 추론/분석 결과물(마크다운 등)도 텍스트 파일로 다루면 동일 방식이 그대로 적용된다.
- **장점**: 기여 귀속을 위한 커스텀 원장(ledger)/DB를 새로 설계할 필요가 없다 — 코딩 생태계가 수십 년간 검증한 git의 authorship 모델을 그대로 얹는 것.

### 2.6. 과제 등록 흐름 — 자유 서술 + 호스트 AI 구조화 초안 + 등록자 승인 게이트

과제 등록자가 목표를 **자유 서술**로 적으면, 호스트 AI가 이를 읽고 **구조화된 "수락 기준 블록"을 자동으로 초안 작성**한다 — 코드 과제면 인터페이스 스펙+테스트 케이스, 추론/분석 과제면 "반드시 다뤄야 할 항목" 체크리스트. **등록자가 이 초안을 직접 확인/수정해서 승인하기 전까지는 과제가 공개되지 않는다**(fail-closed 게이트) — 고정 양식(폼)을 처음부터 강제하는 대신, 자유 서술의 낮은 진입장벽과 구조화된 검증 가능성을 절충한다.

### 2.7. 참여자 수락 절차 — 선착순/호스트 배정 둘 다 지원, 선택은 호스트 몫

과제별로 **선착순(First-come)** 방식과 **호스트 AI 배정형**(참여자 이력/전문 분야 기반 지목) 방식을 둘 다 지원한다. 어느 쪽을 쓸지는 시스템이 강제하지 않고 **과제를 올린 사람(호스트)이 과제별로 직접 선택**한다.

### 2.8. 멀티벤더 통신 — 1차는 A(결과물만 제출), B(실시간 오케스트레이션)는 확장 여지로만 남김

1차 구현 방향은 **A: 참여자가 자기 AI(Claude/GPT/Gemini/로컬 LLM 등 무엇이든)를 알아서 굴려서, 완성된 산출물(git 브랜치/커밋)만 제출**하는 구조로 간다. 호스트 AI와 참여자 AI 사이에 실시간 통신 프로토콜이 필요 없다 — §2.4/2.5의 git 브랜치 흐름만으로 충분하다.

**B(참여자 AI들과 호스트가 실시간으로 과제 위임·상태추적·되묻기를 주고받는 구조)는 지금 만들지 않고 확장 여지로만 기록해둔다.** B로 갈 경우의 기술 스택 후보:

| 계층 | 기술 | 역할 |
|---|---|---|
| 에이전트 간 과제 위임 | **A2A Protocol**(Google 발표, 現 Linux Foundation) | 호스트=A2A 클라이언트, 참여자 AI=A2A 서버. 각 참여자는 "Agent Card"(역량 매니페스트)를 등록, 호스트는 구조화된 task lifecycle로 위임·추적 |
| 참여자 개별 에이전트의 도구 접근 | **MCP** | 참여자 개인 영역(git, 파일 접근 등) — 호스트가 관여할 필요 없음 |
| 호스트 AI의 멀티벤더 모델 호출 | **LiteLLM 프록시** | Claude/GPT/Gemini/로컬LLM을 OpenAI 호환 엔드포인트 하나로 통일 |
| 전송 방식 | JSON-RPC 2.0 over HTTPS + SSE | A2A의 실제 wire format |

**B로 넘어가야 하는 신호**: 호스트가 작업 중간 상태를 실시간 추적해야 하거나, 작업 도중 참여자 AI에게 되물어야 하는 시나리오가 실제로 생길 때.

### 2.9. 컨셉 — 아는 사람끼리 초대제

과제 등록·수락 구조는 불특정 다수에게 열린 공개 게시판이 아니라 **호스트가 아는 사람을 초대하고, 초대받은 사람만 수락 가능한 구조**다.

**위협 모델**: 낯선 참여자의 악의적 제출을 막는 문제가 아니라, 아는 사람의 **실수(부주의한 버그, 안전하지 않은 코드)**를 잡는 문제로 무게중심이 있다. 단, §2.2의 결정론적 검증 자체는 그대로 유지한다 — 참여자 본인이 신뢰할 수 있어도, **참여자가 굴리는 AI 에이전트는 여전히 신뢰할 수 없는 인지(untrusted cognition)**이기 때문이다. 참여자의 AI가 과제 수행 중 외부 콘텐츠를 통해 간접 프롬프트 인젝션에 노출되면, 참여자 본인의 악의와 무관하게 안전하지 않은 코드가 제출될 수 있다.

### 2.10. 포지셔닝 — 완전히 독립된 별개 프로젝트

이 프로젝트는 ModueHarness/ModueAgent의 자매 프로젝트나 확장이 아니라 **완전히 독립된 별개 프로젝트**로 간다. 재사용한 건 설계 원칙이지 코드/런타임이 아니며, 이 프로젝트의 핵심(사람들 간 협업 워크플로우 조율)은 그 두 프로젝트의 원래 문제(단일 에이전트 내 도구 오남용 방지)와는 다른 층위다.

### 2.11. 보상/인센티브 구조는 범위 밖 — 기여 귀속만으로 종료

이 프로젝트는 **기여 귀속(git blame 기반, §2.5)까지만** 책임진다. 금전 보상이나 포인트/크레딧 같은 인센티브 시스템은 설계 범위에 포함하지 않는다 — 아는 사람끼리 초대제(§2.9)라 낯선 사람의 노동력을 금전적으로 유인할 필요성 자체가 줄어든다.

### 2.12. 프로젝트명 — "따로또같이"

각 참여자는 **따로**(자기 AI로, 초대받은 사람들 사이에서, 독립적으로) 작업하지만, 호스트의 검증·병합을 거쳐 **같이**(하나의 결과물로) 완성된다. §2.5의 git blame 기반 귀속도 "따로 기여했지만 같이 만든 결과물 안에 각자의 몫이 그대로 남는다"는 이름의 의미와 정확히 맞아떨어진다.

### 2.13. 구현 전략 — GitHub를 백엔드로, 프론트엔드 앱+AI는 초대/일감나누기 자동화 담당

**GitHub를 실질적 백엔드로 그대로 쓴다**(Issue=과제, PR=제출, Actions=검증, git blame=귀속 — §2.1~2.5, §2.9 전부 GitHub 기본 기능에 대응). 그 위에 **별도 프론트엔드/로컬 에이전트를 얹어서, 초대(§2.9)와 일감나누기(§2.6 수락기준 초안 작성, §2.7 선착순/배정 판단)를 AI로 자동화**한다.

**추천 스택**:

| 계층 | 기술 | 역할 |
|---|---|---|
| GitHub 연동 | **GitHub App** (REST/GraphQL) | Issue/PR 생성, 콜라보레이터(초대) 관리, Checks API로 검증 결과 조회 |
| 검증 자동화 | **GitHub Actions** | AST 보안 스캔(`AgentSecurityAuditor` 패턴 참고, 독립 구현) + 수락 기준(§2.6) 테스트 실행, 결과를 Check run으로 보고 |
| 백엔드(BFF) | 경량 API 서버(Python) | GitHub Webhook 수신 → 재시도/재할당 상태 관리(§2.3/2.4) → 클라이언트에 상태 제공 |
| AI 호출 | LLM API (직접 또는 LiteLLM 프록시) | §2.6 수락기준 초안 작성, §2.7 배정형 선택 시 참여자 이력 기반 추천 |
| 상태 저장 | SQLite (경량) | GitHub에 자연스럽게 없는 것만: 초대 목록, 재시도/재할당 카운터, 참여자 이력 |
| 인증 | GitHub OAuth App | 별도 계정 시스템 없이 git 아이덴티티와 그대로 연결 — §2.5 귀속과 자연히 일치 |
| GitHub→백엔드 통신 | GitHub Webhooks | 폴링 대신 이벤트 기반 |
| 호스트↔참여자 메시지 전달 | 백엔드의 메시지 우편함 API(§2.20) | PKI 암호문(§2.17)을 저장·1회성 전달만 — 별도 릴레이 서버 불필요 |

### 2.14. GitHub 저장소 — `apart-and-together`

프로젝트 이름을 그대로 직역, GitHub 저장소 명명 규칙(공백 불가)에 맞춰 kebab-case로 표기했다.

### 2.15. 구체적 초대 메커니즘 — 각자 PC의 로컬 에이전트 + 암호화된 초대장 문자열 (일부 §2.20으로 대체됨)

> **[갱신]** 아래 "접속 주소"와 "직접 연결(P2P)" 부분은 NAT 문제 때문에 §2.20(백엔드 우편함 방식)으로 대체됐다. 초대장의 OTP/공개키 부분은 그대로 유효하다.

"따로또같이"는 웹앱이 아니라 **각자 PC에 설치하는 로컬 에이전트 프로그램**이다. 초대 흐름:

1. 호스트와 초대받을 사람 둘 다 "따로또같이" 에이전트를 미리 다운로드해서 가지고 있다.
2. 호스트가 **초대장**을 만든다 — 난수화/암호화된 문자열이며, 그 안에 호스트의 ~~접속 주소~~ **공개키**와 OTP성 일회용 정보가 담겨 있다(§2.20).
3. 초대장은 플랫폼이 관여하지 않는 **아웃오브밴드 채널**(메신저, 이메일 등)로 전달된다.
4. 초대받은 사람이 자기 에이전트에 이 초대장 문자열을 입력하면, §2.20의 백엔드 우편함을 통해 호스트와 통신을 시작한다(직접 소켓 연결이 아님).

**보안 원칙**: OTP성 정보는 **일회용·단기 TTL**이어야 한다 — JIT(Just-In-Time) 임시 권한 토큰과 같은 사고방식이다. 초대장 문자열이 메신저 대화에 평문으로 남더라도, 이미 쓰였거나 만료된 초대장은 재사용이 안 되도록 한다.

> **참고: 홀펀칭(NAT hole punching)이란?**(역사적 참고 — 아래 §2.19/§2.20 과정에서 검토했던 배경) 공유기 뒤 두 PC가 포트포워딩 없이 직접 연결하는 기법. 양쪽이 바깥의 중개 서버(STUN)에서 서로의 공인 주소를 알아내 교환한 뒤 거의 동시에 상대에게 패킷을 쏘면 각자의 공유기에 임시 구멍이 생겨 직접 연결이 성사된다. 다만 "symmetric NAT"(CGNAT 등)에서는 이 방식 자체가 안 통해 결국 TURN 릴레이로 폴백해야 한다 — 이런 한계 때문에 최종적으로 §2.20(NAT 문제를 아예 안 만나는 방식)으로 대체됐다.

### 2.16. 알림 채널의 용도 — 온보딩 + 호스트→참여자 실시간 알림(과제 배정/추가 작업) (전송 수단은 §2.20으로 대체됨)

이 채널은 **본인 확인(온보딩)에서 끝나지 않고, 이후에도 유지되며 호스트가 참여자에게 실시간 메시지(과제 배정됨, 추가 작업 있음 등)를 보내 참여자의 AI가 자동으로 반응하게 하는 용도로도 쓰인다.** (원래 P2P 직접 연결로 상정했으나, §2.20에 따라 실제로는 §2.13 백엔드의 메시지 우편함으로 구현된다 — 목적·원칙은 그대로다.)

**§2.13/§2.8과의 관계**: 실제 작업물의 제출·검증·병합은 여전히 GitHub 기반(§2.13)으로 남는다 — 이 채널은 **"GitHub에 뭔가 새로 생겼다"는 사실을 실시간에 가깝게 밀어주는 알림/커맨드 통로**일 뿐, 작업 데이터 자체를 이 채널로 옮기지 않는다(포인터만 전달 — 예: Issue/PR URL). §2.8의 A2A/MCP 풀스택 대신 **이 용도에 맞춘 경량 커스텀 채널**로 대응하는 쪽을 선택했다 — 필요한 만큼만 만든다는 원칙과 일치한다.

**받아들이는 한계**: 참여자의 에이전트가 그 순간 오프라인이면(폴링 중이 아니면) 알림은 유실될 수 있다. 치명적이지 않다 — **GitHub이 여전히 유일한 진실 공급원**이라, 참여자가 알림을 못 받아도 나중에 GitHub을 직접 확인하면 상태는 그대로 남아있다.

### 2.17. 호스트-참여자 간 모든 메시지는 PKI 암호화 통신

호스트-참여자 사이에 오가는 **모든 메시지(온보딩 핸드셰이크 + 이후의 실시간 알림)는 PKI(공개키 기반) 암호화로 주고받는다.** (전송 수단이 §2.20으로 바뀌어도 이 요구사항은 그대로다.)

**구체적 메커니즘**:
1. 호스트와 참여자 각자의 로컬 에이전트가 자기 공개키/개인키 쌍을 갖는다.
2. 초대장 문자열에 호스트의 **공개키(또는 지문)**가 함께 담긴다 — 초대받은 사람의 에이전트가 처음부터 "진짜 그 호스트"와 대화하는지 검증할 수 있게.
3. 초대받은 사람의 에이전트도 초기 핸드셰이크 때 **자기 공개키를 OTP로 인증된 상태에서** 호스트에게 전달한다 — 초대장을 가로챈 제3자가 참여자 행세를 못 하게.
4. 이후 모든 메시지는 상대의 공개키로 암호화하고 자신의 개인키로 서명한다 — 기밀성과 인증을 동시에 보장한다. (구체적으로는 별도의 암호화+서명 2단계가 아니라 하나의 연산으로 처리 — §2.21 참고)

**왜 중요한가**: 전송 경로가 무엇이 되든(현재는 §2.20의 백엔드 우편함), 그 경로는 암호화된 바이트만 중계할 뿐 내용을 읽을 수 없다 — 전송 계층을 신뢰할 필요가 없어진다. §2.20에서도 이 원칙은 유지된다 — **최초 공개키 교환은 반드시 아웃오브밴드 초대장을 통해서만 이뤄지고, 백엔드가 공개키 디렉토리 역할을 하지 않는다**(그러면 백엔드가 공개키를 바꿔치기하는 중간자가 될 수 있다).

**받아들이는 전제**: 이 신뢰 체인의 시작점은 초대장이 전달되는 아웃오브밴드 채널(메신저/이메일) 자체의 신뢰성이다 — "아는 사람끼리" 초대제(§2.9)라는 전제가 여기서도 실질적 방어선 역할을 한다.

**연결되는 후속 아이디어(미결)**: 같은 키 쌍으로 git 커밋도 서명(GPG/SSH commit signing)하면, §2.5의 git blame 귀속이 자기 신고가 아니라 암호학적으로 검증 가능한 귀속이 된다.

### 2.18. "내 AI" ↔ "따로또같이 에이전트" 연결은 MCP

사용자가 평소 쓰는 AI(Claude 등)와 로컬에 설치된 "따로또같이" 에이전트 사이의 연결은 **MCP**로 한다.

**구체적 그림**: "따로또같이" 에이전트가 로컬 MCP 서버로 뜨고, 다음과 같은 도구를 노출한다:
- `list_invitations()` — 받은 초대 확인
- `get_task_details(task_id)` — 수락 기준 블록 조회
- `accept_task(task_id)` — 과제 수락
- `submit_branch(task_id, branch_ref)` — 작업 결과 제출
- `get_verification_result(task_id)` — 검증 통과/실패 확인
- `get_reassignment_context(task_id)` — 재할당 시 이전 시도 내용/실패 사유 조회

**명확히 구분해야 할 경계**: 이건 "내 AI ↔ 내 로컬 에이전트"의 수직적(1:1 모델-도구) 관계에만 해당한다. **"호스트 에이전트 ↔ 참여자 에이전트"끼리의 통신(§2.15~2.17, §2.20)은 여전히 MCP가 아니다** — MCP는 여러 참여자 간 통신/PKI 개념이 없는 프로토콜이라, 그 통신은 커스텀 채널로 그대로 남는다.

### 2.19. NAT 처리 방식 — 자체 경량 릴레이 (§2.20으로 대체됨)

> **[갱신] 이 결정은 전면 폐기되고 §2.20으로 대체됐다.** 릴레이를 상시 프로세스가 아닌 서버리스 플랫폼(Vercel 등)에 올리려다, 애초에 §2.13에서 이미 만들기로 한 백엔드에 우편함 기능만 얹으면 릴레이 자체가 필요 없어진다는 결론에 도달했다.

### 2.20. 메시지 전송 수단을 P2P/자체 릴레이에서 §2.13 백엔드 우편함으로 대체

§2.15(초대장 접속주소+P2P 직접연결)와 §2.19(자체 릴레이)를 전부 대체한다. **새 릴레이 서버를 만들지 않고, §2.13에서 이미 만들기로 한 백엔드(GitHub Webhook 수신용으로 어차피 공개 접속 가능해야 하는 서버)에 "메시지 우편함" 기능만 추가한다.**

**데이터 모델** (§2.13의 기존 SQLite에 테이블 하나 추가):

```
messages
├─ message_id    (UUID)
├─ sender_id     (호스트의 GitHub user ID)
├─ recipient_id  (참여자의 GitHub user ID)
├─ ciphertext    (§2.17로 암호화된 블롭 — 백엔드는 내용을 못 읽음)
├─ created_at
└─ expires_at    (짧은 TTL, 예: 48시간 — 안 가져가면 자동 삭제)
```

**API 2개**:
- `POST /messages` — 호스트 에이전트가 (GitHub OAuth로 인증) `{recipient_id, ciphertext}` 제출. 백엔드는 저장만 함.
- `GET /messages/poll` — 참여자 에이전트가 (GitHub OAuth로 인증) 주기적으로 호출해 자기 앞 메시지를 가져감. 가져가면 즉시 삭제(1회성 전달, 큐잉 없음). 지연을 줄이려면 **롱폴링**(최대 20~30초 대기 후 응답)도 가능 — 백엔드가 상시 프로세스라 문제없음(서버리스 플랫폼에서는 실행시간 제한에 걸린다).

**신원 확인은 새로 안 만듦**: `sender_id`/`recipient_id`는 §2.13에서 이미 정한 GitHub OAuth 신원(GitHub user ID)을 그대로 사용한다.

**신뢰 모델은 §2.17 그대로 유지 — 바뀌는 건 전송 계층뿐**: 최초 공개키 교환(초대장 + OTP 인증)은 여전히 아웃오브밴드로만 이뤄진다. 백엔드가 공개키 디렉토리 역할을 하지 않는다 — 그러면 백엔드가 공개키를 바꿔치기하는 중간자가 될 수 있어 §2.17이 막으려던 문제가 되살아나기 때문이다. 백엔드는 "이미 서로 공개키를 아는 두 사람 사이의 암호문"을 전달만 하는 우편함 역할만 한다.

**왜 이 방향이 나은가**:
- NAT 문제 자체가 사라진다 — 양쪽 다 이미 공개적으로 떠 있는 백엔드로 **바깥을 향해** HTTPS 요청만 하면 되니, 공유기/CGNAT 환경과 무관하게 항상 동작한다.
- 초대장의 "접속 주소" 필드가 필요 없어진다 — 백엔드 URL은 이미 고정이고 다들 안다.
- 릴레이 서버 운영/호스팅 부담이 통째로 사라진다.
- §2.13에서 이미 정당화된 인프라를 재사용하는 것뿐이라, "필요한 만큼만 만든다"는 원칙과 가장 잘 맞는다.

**받아들이는 것**: 진짜 즉시 push가 아니라 폴링 간격만큼의 지연(수 초~1분)이 생긴다 — §2.16에서 이미 "이 알림은 빠른 길일 뿐 유일한 길이 아니다"라고 정해뒀으므로 설계 취지에 어긋나지 않는다.

**Phase 계획과의 관계**: 이 기능은 §2.13의 백엔드(Phase 2)가 있어야 동작한다 — Phase 0/1엔 애초에 봇이 없었으니 자연스러운 제약이다.

### 2.21. PKI 구체 구현 확정 — libsodium(PyNaCl), 세션이 아닌 메시지 단위 암호화

**전제 변화**: §2.20에서 전송 방식이 실시간 P2P 소켓에서 비동기 우편함(폴링)으로 바뀌었다. Noise Protocol Framework류(실시간 상호 핸드셰이크 전제)는 더 이상 어울리지 않는다 — 필요한 건 **메시지 하나하나를 독립적으로 암호화하는 방식(per-message sealed encryption)**이다.

**확정 사항**:
- **라이브러리**: `libsodium`(Python은 `PyNaCl`) — 암호 프리미티브를 직접 조합하지 않고 검증된 라이브러리를 그대로 사용.
- **암호화**: `crypto_box`(X25519 + XSalsa20-Poly1305). 발신자 개인키 + 수신자 공개키로 메시지를 봉인하면, 수신자는 자기 개인키 + 발신자 공개키로만 열 수 있다 — 이 한 연산이 기밀성과 발신자 인증을 동시에 처리한다.
- **서명용 별도 키**: `Ed25519`. 메시지 인증은 `crypto_box`로 이미 되지만, 이 키는 git 커밋 서명에도 그대로 쓴다 — 최신 git은 SSH 키(Ed25519 포함)로 커밋 서명을 지원하므로, 에이전트가 만드는 키 하나로 메시지 인증과 커밋 서명을 겸할 수 있다.
- **키 수명**: 세션마다 새로 만들지 않고 에이전트 설치 시 1회 생성해 계속 재사용한다(SSH 키와 같은 방식).
- **초대장 인코딩**: 공개키(32바이트)를 base64url로 인코딩(약 43자). §2.20에서 "접속 주소" 필드가 빠지면서 초대장 문자열이 더 짧아졌다.

### 2.22. git 커밋 서명 필수화 — §2.21의 Ed25519 키 재사용

**§2.5(git blame 귀속)의 자기신고 약점을 닫기 위해 커밋 서명을 필수로 한다.**

- 최신 git의 SSH 커밋 서명 기능(`gpg.format=ssh`)에 §2.21에서 이미 생성한 Ed25519 키를 그대로 사용 — 추가 키 생성 없음.
- **서명키 발급부터 등록까지 전부 자동화**: 로컬 에이전트가 최초 실행 시(§2.21) 키를 자동 생성하고, 온보딩 시 그 공개키를 참여자의 GitHub 계정에 Signing Key로 자동 등록(GitHub API)한다 — 사용자가 직접 키를 만들거나 GitHub 설정 화면에서 복붙할 필요가 전혀 없다.
- §2.2의 검증 파이프라인(Phase 1 GitHub Actions)에 "커밋이 등록된 서명키로 정상 서명됐는가" 체크를 추가 — 미서명 커밋은 검증 실패 처리.

### 2.23. 로컬 에이전트 배포 방식 — 일단은 저장소 클론(PyInstaller는 나중으로 미룸)

**1차는 PyInstaller 단일 실행파일 대신, `apart-and-together` 저장소를 그냥 git clone해서 로컬에서 직접 실행하는 방식으로 간다.**

**왜**: 지금 단계(Phase 0~1, 실제 지인 드라이런)에서 참여할 사람들은 애초에 git/Python을 다룰 수 있는 사람들일 가능성이 높고, 빌드 파이프라인을 지금 갖추는 건 아직 검증도 안 된 워크플로우에 과한 투자다.

**미룬 것(나중 업그레이드 경로)**: 비개발자 지인도 참여시키고 싶어지는 시점(대략 Phase 3)이 오면, 그때 PyInstaller+GitHub Releases 배포로 전환한다.

**업데이트**: 클론 방식이므로 `git pull`이 곧 업데이트 — 별도 자동 업데이트 메커니즘이 필요 없다.

---

## 3. 엔드투엔드 시나리오 (End-to-End Scenario)

지금까지의 결정을 하나의 흐름으로 엮은 가상 예시. 등장인물: 과제를 올린 **지민(호스트)**, 초대받은 **서연**과 **도윤**.

### 3.1. 과제 등록 (§2.6, §2.9)

1. 지민이 "따로또같이"에 새 목표를 자유 서술로 적는다: *"우리 프로젝트에 CSV 업로드 기능을 추가하고 싶어. 업로드된 파일을 파싱해서 DB에 저장하는 부분이 필요해."*
2. 지민이 서연과 도윤을 **초대**한다. 둘 다 서로 아는 사이 — 불특정 다수에게 공개되지 않는다.
3. 호스트 AI(지민의 AI)가 이 서술을 읽고 **수락 기준 블록 초안**을 자동으로 작성한다:
   - 인터페이스: `parse_csv(file_path: str) -> list[dict]`
   - 테스트 케이스: 정상 CSV 3건, 빈 파일 1건, 헤더만 있는 파일 1건, 인코딩 깨진 파일 1건(에러 처리 확인)
   - 비고: DB 저장 시 SQL 인젝션 방지 필수
4. 지민이 초안을 확인하고 테스트 케이스 하나("컬럼 수 불일치 시 에러 처리")를 추가해서 **승인** — 이 승인 전까지는 서연/도윤에게 과제가 노출되지 않는다(fail-closed 게이트).

### 3.2. 참여자 확정 (§2.7)

5. 지민은 이 과제를 "선착순"으로 열기로 선택한다(호스트 재량). 서연이 먼저 수락해서 과제를 맡는다.

### 3.3. 작업 및 제출 (§2.8)

6. 서연은 자기 AI로 `parse_csv()`를 구현하고, 자기 브랜치(`feat/csv-upload-seoyeon`)에 커밋한 뒤 제출한다. 지민의 호스트 AI는 서연이 내부적으로 AI를 어떻게 썼는지 알 필요가 없다 — 완성된 브랜치만 받는다.

### 3.4. 검증 — 실패 경로 (§2.1, §2.2, §2.3, §2.4)

7. 호스트 AI가 병합 전 검증한다:
   - **안전성 검증(결정론적 AST 스캔)**: 통과.
   - **과제 적합성 검증**: 6개 테스트 케이스 중 "인코딩 깨진 파일" 케이스에서 예외가 처리되지 않고 그대로 크래시 — **실패**.
8. 서연에게 **재시도 기회 1회**가 주어지고, 실패 사유가 구조화되어 전달된다.
9. 서연이 재시도했지만 다시 같은 지점에서 실패 — **재시도 상한 소진**.
10. 지민이 도윤에게 **재할당**한다. 서연의 브랜치와 실패 사유가 **통째로 도윤에게 공개**된다. 도윤은 그 브랜치에서 새 브랜치를 파서 이어서 작업한다.

### 3.5. 검증 — 성공 및 병합 (§2.1, §2.5)

11. 도윤이 제출한 버전은 모든 테스트 케이스와 AST 스캔을 통과 — 호스트 AI가 메인 브랜치에 병합한다.
12. 병합된 `parse_csv()` 함수를 `git blame`으로 보면: 함수 본체 대부분은 **서연**, 인코딩 예외처리 블록은 **도윤**으로 정확히 표시된다.

### 3.6. 종료 (§2.11)

13. 과제가 완료됐다는 알림이 지민/서연/도윤에게 간다. 포인트나 금전 보상은 없다 — git 커밋 기록에 각자 이름이 남은 것으로 종료한다.

---

## 4. 구현 계획 (Implementation Plan)

한 번에 다 만들지 않고, 단계별로 쌓는다.

### Phase 0 — 수동 드라이런 (자동화 없음)
실제 지인 그룹으로 §3의 시나리오를 손으로 한 번 돌려본다. **완료 기준**: 과제 1건이 등록→수락→제출→(필요시 재할당)→병합까지 실제로 완주.

### Phase 1 — 검증 자동화 (GitHub Actions만)
AST 위험 호출 탐지 패턴을 참고해 독립적으로 새로 작성, GitHub Actions로 등록. 1차는 Python 과제로 한정. 재시도/재할당은 아직 사람이 수동으로 처리.

### Phase 2 — 호스트 AI 봇 (GitHub App + 경량 백엔드)
수락 기준 초안 자동 작성과 재시도/재할당 자동화. 아직 프론트엔드 없음.

### Phase 3 — 로컬 에이전트/프론트엔드
초대(§2.15/2.16)와 참여자 확정을 감싸는 실제 클라이언트 추가.

---

## 5. 아직 다루지 않은 미결 항목 (Open Questions)

- [x] ~~PKI 구체 구현~~ → §2.21에서 확정(libsodium/PyNaCl, X25519 `crypto_box` + Ed25519 서명키)
- [x] ~~git 커밋 서명으로 §2.5 귀속을 암호학적으로 검증 가능하게 할지~~ → §2.22에서 확정(필수화, 키 발급·등록 전부 자동화)
- [x] ~~로컬 에이전트 배포 방식~~ → §2.23에서 확정(1차는 저장소 클론, PyInstaller+GitHub Releases는 Phase 3 이후로 미룸)

**2차 라운드 미결 항목도 전부 해소됨(§2.15~2.23).**

---

## 6. 관련 프로젝트

- [ModueHarness](https://github.com/JeaMinLim/ModueHarness) — 에이전트 OS 마이크로커널. 이 프로젝트가 참고한 검증 트랩·자원 예산 원칙의 원본.
- [ModueAgent](https://github.com/JeaMinLim/ModueAgent) — ModueHarness 원칙을 이식한 단일 프로세스 라이브러리. `AgentSecurityAuditor` AST 스캔 패턴의 원본.
